# init · 디자인 시스템 가이드

> **init** — "인터뷰를 잇다". AI 면접 플랫폼.
> 본 문서는 `init_wireframe.html`의 24개 화면에서 확인한 디자인 토큰과 실제 배치 규칙을 정리한 가이드입니다. 이후 모든 화면은 이 토큰과 패턴을 따릅니다.

---

## 1. 무드 & 원칙

- **흰 배경 + 인디고/바이올렛 글로우.** 밝고 깨끗한 화이트 캔버스 위에 보라빛 라디얼 글로우로 트렌디한 에너지를 더합니다.
- **젊고 모던하게.** 큰 타이포그래피, 넉넉한 여백, 부드러운 그라데이션, pill(완전 둥근) 버튼.
- **절제.** 색은 한 계열(인디고→블루)만 강조색으로 쓰고 나머지는 무채색. 장식보다 콘텐츠.
- **신뢰감.** AI 평가 플랫폼인 만큼 과하지 않고 정돈된 인상.

---

## 2. 컬러 토큰

### Brand (강조)
| 토큰 | 값 | 용도 |
|---|---|---|
| `--brand-1` | `#6E5BF6` | 메인 인디고 (그라데이션 시작) |
| `--brand-2` | `#5B8CFF` | 블루 (그라데이션 끝) |
| `--brand-strong` | `#5B57F2` | 단색 인디고 (세그먼트 활성 텍스트 등) |
| `--brand-grad` | `linear-gradient(120deg,#6E5BF6,#5B8CFF)` | 주요 버튼·강조 요소 |
| `--brand-soft` | `#EEEDFB` | 연한 보라 배경 (배지, 아이콘 칩) |

### Ink (텍스트)
| 토큰 | 값 | 용도 |
|---|---|---|
| `--ink-900` | `#14131C` | 헤드라인 / 최강조 |
| `--ink-800` | `#16151F` | 제목 |
| `--ink-700` | `#3A384A` | 라벨 |
| `--ink-500` | `#5C5A6C` | 본문 |
| `--ink-400` | `#6A6878` | 보조 본문 |
| `--ink-300` | `#807E8E` | 캡션 |
| `--ink-200` | `#A7A4B8` | placeholder / 비활성 |

### Surface & Line
| 토큰 | 값 | 용도 |
|---|---|---|
| `--bg` | `#FFFFFF` | 기본 배경 |
| `--bg-tint` | `linear-gradient(180deg,#F7F6FF 0%,#FBFAFF 38%,#fff 100%)` | 인증 화면 배경 |
| `--surface-soft` | `#F6F5FD` | 미니 UI 카드 안쪽 |
| `--line` | `#E5E3F0` | 인풋 보더 |
| `--line-soft` | `#ECEBF3` | 카드 보더 / 디바이더 |

### Glow (라디얼)
```css
/* 히어로 글로우 */
background: radial-gradient(closest-side,
  rgba(123,107,255,.40), rgba(91,140,255,.18) 48%, transparent 72%);
filter: blur(14px);
```
보조 글로우는 `rgba(123,107,255,.22)` / `rgba(91,170,255,.20)` 사용.

---

## 3. 타이포그래피

- **본문 서체:** `Pretendard` (Variable). `-apple-system` 폴백.
- **모노 악센트:** `JetBrains Mono` — 영문 라벨(예: `AI INTERVIEW PLATFORM`, `AI REPORT`)에만 소량 사용. `letter-spacing:.22em` + 대문자.

| 역할 | size / weight / line | letter-spacing |
|---|---|---|
| 랜딩 Hero (H1) | 72px / 800 / 1.02 | -.05em |
| 랜딩 Section (H2) | 32px / 800 | -.03em |
| 내부 페이지 제목 | 24px / 800 | -.02em |
| 카드 제목 (H3/H4) | 16–20px / 700 | — |
| 폼 제목 | 24–25px / 800 | -.02em |
| 본문 | 15–18px / 400 / 1.6 | — |
| 라벨 | 13px / 600 | — |
| 캡션·모노라벨 | 12px / 600 | .22em (모노만) |

**그라데이션 텍스트** (강조 단어):
```css
background: linear-gradient(120deg,#6E5BF6,#5B8CFF);
-webkit-background-clip:text; background-clip:text; color:transparent;
```

---

## 4. 스페이싱 & 라운딩

- **라운딩:** 인풋/버튼 `11–12px` · 카드 `18–24px` · pill 버튼·배지 `999px` · 클로징 배너 `28px`.
- **컨테이너 최대폭:** 랜딩 히어로 `860px` · 랜딩 콘텐츠 `1000px` · 앱 내부 본문 `1200px` · 폼 카드 `444–540px` · 모달 `520px`.
- **섹션 상하 여백:** 64–96px 리듬.
- **그림자:**
  - 카드: `0 12px 36px rgba(20,18,45,.05)`
  - 폼 카드: `0 24px 64px rgba(40,30,90,.10)`
  - 강조 버튼: `0 10px 26px rgba(91,110,246,.32)`

---

## 5. 핵심 컴포넌트

### 로고
공식 로고 이미지를 사용합니다 — **`assets/logo-init.png`** (1010×375, PNG).

![init 로고](assets/logo-init.png)

- **구성:** 좌측 심볼(두 사람 + 말풍선 "···") + 우측 워드마크 `init`(다크 잉크 `#16151F` 계열).
- **심볼 의미:** 두 사람을 잇는 대화 = "인터뷰를 잇다". 말풍선·인물에 인디고→블루 그라데이션 사용 → 브랜드 컬러와 일치.
- **사용 규칙**
  - 흰/밝은 배경 위에 사용. 어두운 배경이 필요하면 흰색 반전 버전을 별도 제작.
  - 최소 폭 약 88px 이상(워드마크 가독성 확보), 좌우 여백은 심볼 높이의 ~30% 확보.
  - 비율 고정(1010:375). 임의로 늘리거나 색을 바꾸지 않습니다.
  - 아이콘만 필요한 자리(파비콘·앱 아이콘 등)에는 좌측 **심볼만** 잘라서 사용.
- **구현 예시**
  ```html
  <img src="assets/logo-init.png" alt="init" style="height:32px;width:auto" />
  ```

### 버튼
- **Primary:** `--brand-grad` 배경, 흰 텍스트, 600, 강조 그림자. (메인 액션)
- **Dark:** `#16151F` 배경, 흰 텍스트, pill. (랜딩 nav·히어로 CTA)
- **Secondary:** 흰 배경 + `--line` 보더, `#2A2836` 텍스트. hover `#FAFAFC`.
- 높이: 폼 버튼 50px / pill CTA 패딩 `14px 28px`.

### 입력 필드
높이 48px, 보더 `--line`, 라운딩 11px. **포커스:**
```css
border-color:#7E7EFF; box-shadow:0 0 0 3px rgba(110,91,246,.14);
```
비밀번호 필드는 우측에 "보기/숨기기" 토글 텍스트(`--brand-1`, 600 13px).

### 세그먼트 토글 (기업/지원자)
`#F4F3FB` 트랙 + 활성 탭은 흰 배경 + `#5B57F2` 텍스트 + 미세 그림자.

### 선택 카드 (회원가입 유형)
기본: 흰 배경 + `#EAE9F2` 보더. **선택 시:** `#F4F3FE` 배경 + `#6E5BF6` 1.5px 보더 + `0 12px 30px rgba(91,87,242,.16)` 그림자 + 우상단 그라데이션 체크 뱃지.

### 미니 UI 카드 (랜딩 "무엇을 하나요")
실제 제품 UI를 축소해 보여줌 — 채팅 버블, 역량 평가 막대그래프. 안쪽 면은 `--surface-soft`.
- 채팅: AI 버블(흰 배경+보더, 좌상단 각짐) / 사용자 버블(`--brand-grad`, 우상단 각짐) / "답변 녹화 중" 펄스 점.
- 리포트: 7px 높이 막대, 트랙 `#E9E7F5`, 채움 `--brand-grad`.

### 클로징 배너
`linear-gradient(120deg,#5B57F2,#6E7BFF 55%,#5B8CFF)` 풀컬러 + 하단 화이트 글로우. 흰 텍스트, 흰 pill 버튼(텍스트 `#5B57F2`).

---

## 6. 모션

```css
@keyframes initpulse { 0%,100%{opacity:1;transform:scale(1)} 50%{opacity:.3;transform:scale(.8)} }   /* 녹화 점 */
@keyframes initdrift { 0%,100%{transform:translate(-50%,0)} 50%{transform:translate(-50%,-26px)} }     /* 히어로 글로우 떠다님 9s */
@keyframes initdrop  { from{opacity:0;transform:translateY(-6px)} to{opacity:1;transform:translateY(0)} } /* GNB 호버 드롭다운 .16s */
```
- 호버 전환: `transition:.15s`.
- 버튼 hover: `opacity:.92`(그라데이션) 또는 배경 톤 변화.

---

## 7. 간격 & 그리드 규칙

- **간격 스케일(4px 베이스):** `4 · 8 · 12 · 16 · 20 · 24 · 32 · 40 · 48 · 64 · 80 · 96`px. 이 외 값은 쓰지 않습니다.
  - 요소 내부 컴포넌트 간: 8–16px · 카드 패딩: 24–38px · 섹션 간: 64–96px.
- **콘텐츠 최대폭:** 랜딩 히어로 `860px` · 랜딩 콘텐츠 `1000px` · 앱 내부 본문 `1200px` · 폼 카드 `444–540px`. 좌우 거터 `24–32px`.
- **앱 그리드:** 화면 목적에 따라 2열/3열 유틸리티를 재사용합니다. `.grid-2`/`.grid-3`는 `gap:16px`, 카드형 2열(`.cards-2`)과 KPI/미니 카드 그리드는 `gap:18px`.
- **반응형 브레이크포인트:**
  | 이름 | 폭 | 동작 |
  |---|---|---|
  | `mobile` | <= 760px | 주요 그리드 1컬럼, 페이지 패딩 `22px 18px 32px`, GNB 축약 |
  | `desktop` | > 760px | GNB + 본문 `max-width:1200px`, 2열/3열 그리드 유지 |
- 터치 타깃 최소 44px, 본문 최소 14px.

---

## 8. 앱 레이아웃 골격 (로그인 이후 내부 페이지)

인증 화면은 "중앙 카드형"이고, 로그인 후 내부 페이지는 **상단 GNB(전역 네비) + 본문** 구조입니다. 좌측 사이드바(SNB)는 사용하지 않습니다. 1차 메뉴는 GNB에 가로로 놓이고, 2차 메뉴는 **호버 시 아래로 펼쳐지는 드롭다운(메가 메뉴)** 으로 노출합니다.

```
┌──────────────────────────────────────────────┐
│  GNB (64px)  로고  채용관리 지원자 리포트 …  🔔 ◉ │
├──────────────────────────────────────────────┤   ▢ 호버 시 1차 메뉴 아래로
│  Page header (제목 + 액션)                      │      드롭다운이 펼쳐짐
│  ────────────────────────────────────────     │
│  Content (max 1200px, 좌우 거터 32px)           │
└──────────────────────────────────────────────┘
```

### GNB (상단 전역 네비)
- 높이 `64px`, 배경 `#FFFFFF`, 하단 보더 `--line-soft`, `position:sticky; top:0`. 내부 페이지라 **글로우 없음**.
- 좌측: 로고(`assets/logo-init.png`, height 26–28px). 로고와 1차 메뉴 사이 간격 `40px`.
- 중앙/좌: 1차 메뉴 항목을 가로 나열(`gap:4px`). 각 항목 높이 64px(GNB와 동일)·패딩 `0 14px`·폰트 `14.5px/600`.
  - 기본 텍스트 `--ink-500`. hover/활성 텍스트 `--brand-strong`(`#5B57F2`).
  - **활성 1차 메뉴:** 항목 하단에 2px `--brand-grad` 언더라인(GNB 바닥에 붙임).
  - 하위 메뉴가 있는 항목은 라벨 우측에 작은 ⌄ 캐럿(8px). hover 시 180° 회전.
- 우측: 알림 벨(38px 라운드 버튼) + 프로필 아바타(38px 원형, `--brand-grad`).

### 호버 드롭다운 (메가 메뉴)
- 트리거: 1차 메뉴 항목에 마우스 호버. **트리거 + 패널을 한 래퍼로 감싸** 패널 위로 이동해도 닫히지 않게 함(마우스가 래퍼를 벗어나면 닫힘). 키보드 포커스 시에도 열림.
- 패널: GNB 바로 아래 부착(`top:64px`). 흰 배경, 라운딩 `14px`, 보더 `--line-soft`, 그림자 `0 16px 40px rgba(20,18,45,.12)`, 패딩 `10px`. 좌측 정렬(트리거 시작점 기준), 최소폭 `220px`.
- 항목: 높이 `40px`, 라운딩 `10px`, 패딩 `0 12px`, 폰트 `14px/500` `--ink-700`. hover 배경 `#F6F5FD` + 텍스트 `--brand-strong`. 필요 시 좌측 아이콘 + 우측 보조설명(12px/`--ink-300`).
- 넓은 메뉴(메가)는 패널 안에서 2–3컬럼 그리드(`gap:4px 24px`)로 묶고, 그룹 제목(11px/600/`--ink-200`, 대문자)을 위에 둡니다.
- 모션: 열림 `opacity 0→1 / translateY(-6px→0)` `.16s`. (드롭다운 키프레임은 §6 참고)

### 페이지 헤더
- GNB 아래 본문 최상단. 좌측: 페이지 제목(24px/800, -.02em) + 보조 설명(14px/`--ink-300`). 우측: 1차 액션 버튼(Primary).
- 하위 탭이 있으면 헤더 아래 탭 바 배치.

> **모바일(sm):** GNB 1차 메뉴는 햄버거 버튼으로 접고, 탭하면 전체화면 메뉴(아코디언으로 2차 메뉴 펼침)로 대체합니다. 호버가 없는 환경이므로 **탭으로 열고 닫습니다.**

---

## 9. 데이터 표시 & 피드백 컴포넌트

### 상태 칩(Badge)
높이 24–26px, 라운딩 999px, 폰트 12px/600. 색은 의미별 soft 배경 + 진한 텍스트.
| 의미 | 배경 | 텍스트 |
|---|---|---|
| 진행/정보 (info) | `#EEEDFB` | `#5B57F2` |
| 완료/성공 (success) | `#E4F7EC` | `#1F8A5B` |
| 대기/주의 (warning) | `#FFF3DC` | `#9A6A00` |
| 실패/위험 (danger) | `#FCE8E8` | `#C8392F` |
| 중립 (neutral) | `#F1F0F6` | `#6A6878` |

### 테이블
- 헤더: 배경 `#FBFAFF`, 텍스트 12.5px/600 `--ink-400`, 하단 보더 `--line-soft`.
- 행: 높이 56px, 셀 14px/400 `--ink-700`, 행 구분 보더 `--line-soft`, hover `#FBFAFF`.
- 라운딩: 표 전체를 16px 카드로 감싸고 `overflow:hidden`. 우측 끝 액션은 케밥(⋯) 또는 텍스트 버튼.
- **리스트(모바일)**: 표 대신 카드 리스트로 — 각 항목 카드(보더 `--line-soft`, 라운딩 14px, 패딩 16px).

### 통계/지표 카드 (KPI)
흰 카드(`0 6px 20px rgba(20,18,45,.04)`), 라벨(13px/`--ink-300`) + 수치(28–32px/800 `--ink-900`) + 증감 칩(success/danger).

### 모달 / 다이얼로그
- 오버레이 `rgba(16,15,31,.45)` + `backdrop-filter:blur(2px)`.
- 패널: 흰 배경, 라운딩 20px, 패딩 28–32px, 그림자 `0 24px 64px rgba(40,30,90,.18)`, 최대폭 `480–560px`.
- 헤더(제목 20px/800) + 본문 + 우하단 액션(Secondary + Primary).

### 토스트(알림)
우상단 또는 하단중앙. 흰 카드 + 좌측 상태 컬러 도트 + 텍스트 14px. 라운딩 12px, 그림자 `0 10px 30px rgba(20,18,45,.12)`. 3–4초 후 페이드.

### 빈 화면 (Empty state)
중앙 정렬: 연한 보라 원형 아이콘 칩(`--brand-soft`) + 제목(17px/700) + 안내(14px/`--ink-300`) + 1차 액션 버튼.

### 탭
하단 보더 트랙(`--line-soft`) 위에 활성 탭만 `--brand-strong` 텍스트 + 2px 인디고 언더라인. 비활성 `--ink-400`.

### 폼 보조 상태
- 에러: 인풋 보더 `#E5897F`, 하단 헬퍼 텍스트 12.5px `#C8392F`.
- 성공: 보더 `#7FC8A4`. 비활성: 배경 `#F6F5FD`, 텍스트 `--ink-200`, 커서 `not-allowed`.

---

## 10. 와이어프레임 기반 실제 배치 규칙

기준 파일: `/Users/ssoming/Downloads/init_wireframe.html`. 아래 규칙은 화면을 예쁘게 보이게 하는 장식값이 아니라, 와이어프레임 24개 화면에서 반복 사용된 실제 레이아웃 패턴입니다.

### 제품 화면과 검토용 셸 분리
- `.showbar`, `.group`, `.shot`, `.device`, `.bezel`, `.chrome`, `.base`, `.review-nav`는 와이어프레임 검토용 프레임입니다. 실제 제품 구현에는 포함하지 않습니다.
- 제품 화면은 `.app` 이하만 기준으로 삼습니다. 진입/인증 화면은 `.app.auth`, 로그인 이후 화면은 `.app` + `.gnb` + `.page`, 면접 진행 화면은 `.iv-top` + `.iv-body`가 기본입니다.

### 랜딩 / 인증 화면
- 랜딩과 인증 화면만 `--bg-tint`와 라디얼 글로우를 사용합니다. 내부 업무 화면에는 글로우를 쓰지 않습니다.
- 랜딩 nav는 높이 `64px`, 좌우 패딩 `30px`, 로고와 링크를 양끝 정렬합니다. 링크 그룹은 `gap:22px`.
- 히어로는 중앙 정렬, `max-width:860px`, `padding:70px 24px 40px`. H1은 72px/800/1.02, CTA는 가운데 `display:flex; gap:10px`.
- 랜딩 콘텐츠 섹션은 `max-width:1000px`, `padding:36px 24px 8px`. 미니 UI는 2열(`1fr 1fr`, `gap:18px`), 기능 카드는 3열(`repeat(3,1fr)`, `gap:18px`).
- 인증 카드 래퍼는 `min-height:520px`, `padding:56px 24px`, 상단 기준 중앙 정렬입니다. 폼 카드는 기본 `max-width:444px`, 넓은 폼은 `540px`, 패딩 `34px`, 라운딩 `24px`.
- 인증 폼에서 2개 입력을 나란히 놓을 때는 `.grid-2`(`gap:16px`), 입력+버튼 조합은 `.inline`(`gap:8px`)을 사용합니다. 회원가입 유형 선택은 `.choice-grid` 2열, 모바일에서는 1열입니다.

### 내부 페이지 기본 골격
- 내부 페이지는 항상 상단 GNB 64px + 본문 `.page` 조합입니다. 사이드바 레이아웃은 사용하지 않습니다.
- `.page`는 `max-width:1200px`, `margin:0 auto`, `padding:28px 32px 44px`.
- `.page-head`는 제목/설명과 우측 액션을 양끝 정렬합니다. `gap:16px`, `margin-bottom:22px`; 제목은 24px/800, 설명은 14px.
- 주요 콘텐츠는 `.panel` 단위로 쌓습니다. 패널은 흰 배경, `--line-soft` 보더, 라운딩 `18px`, 패딩 `24px`, `margin-bottom:18px`.
- 패널 헤더가 필요한 경우 `.panel-head`를 쓰고 제목과 액션을 양끝 정렬합니다. `gap:10px`, `margin-bottom:16px`.
- 내부 그리드는 실제 CSS와 동일하게 `.grid-2`=`1fr 1fr`/`gap:16px`, `.grid-3`=`repeat(3,1fr)`/`gap:16px`, `.cards-2`=`1fr 1fr`/`gap:18px`를 재사용합니다.
- 필터/액션 묶음은 `.toolbar`로 배치합니다. `display:flex`, `flex-wrap:wrap`, `gap:8px`, `align-items:center`.

### 데이터 화면 배치
- KPI가 있는 화면은 본문 시작에 `.kpi-row`를 배치합니다. 3열 고정(`repeat(3,1fr)`), `gap:18px`, 아래 여백 `18px`.
- 목록형 공고 행은 `.posting` 패턴을 사용합니다. 그리드 컬럼은 `48px 1fr auto auto auto`, `gap:16px`, `padding:16px`; 좌측 로고 48px, 중앙 제목/메타, 우측 숫자/상태/액션 순서입니다.
- 카드형 채용공고 목록은 필터 패널 다음에 `.cards-2` + `.jobcard`를 사용합니다. `jobcard`는 패딩 `22px`, 라운딩 `16px`, 내부 헤더는 로고 40px + 제목 `gap:12px`.
- 표는 단독으로 노출하지 않고 항상 `.table-wrap` 안에 넣습니다. 표 위에는 필요하면 `.panel-head` 또는 `.toolbar`를 두고, 표 아래 페이지네이션은 `.pagination`으로 좌측 정보, 중앙 페이지 버튼, 우측 개수 선택을 배치합니다.
- 비교/대기/설명성 영역은 `.ph-box`를 사용합니다. 실제 데이터가 없거나 비활성 조건이 있는 경우에도 빈 영역을 유지해 레이아웃 높이가 크게 흔들리지 않게 합니다.

### 폼 / 설정 화면 배치
- 생성/설정 화면은 `page-head` 다음에 폼 패널을 세로로 쌓습니다. 한 패널 안에서 기본 입력은 2열, 긴 텍스트/업로드/동의는 전체 폭을 사용합니다.
- 프로젝트 생성, 지원자 등록, 기업/지원자 마이페이지처럼 속성이 많은 폼은 `.grid-2`로 필드를 묶고, 저장/등록 버튼은 그리드 아래 좌측에 둡니다.
- 모의면접 설정처럼 선택값 3개가 동등할 때만 `.grid-3`를 사용합니다. 모바일에서는 1열로 접힙니다.
- 파일 업로드는 `.upload` 패턴으로 고정합니다. 보더는 `1.5px dashed --line`, 패딩 `16px`, 아이콘 칩 38px, 완료 상태는 우측 `margin-left:auto`.
- 단계형 지원서 제출은 상단 요약 패널 안에 3열 요약 필드와 `.steps`를 먼저 놓고, 이후 `cards-2`로 기본 정보/서류 업로드, 추가 설명/상태 점검을 2단으로 배치합니다. 마지막 액션 바는 `.toolbar`를 쓰며 최종 제출 버튼만 `margin-left:auto`.

### 리포트 / 평가 상세 배치
- 리포트 상세는 단일 대시보드가 아니라 패널을 순서대로 쌓습니다: 점수 표 -> 평가 근거 -> 영상/스크립트 동시 조회 -> 보조 지표.
- 점수 시각화는 표의 마지막 컬럼에 `.progress`를 넣습니다. 역량 컬럼은 약 160px, 점수 컬럼은 약 70px로 좁게 두고, 막대가 남은 폭을 차지합니다.
- 영상/스크립트 동시 조회는 `.iv-grid`를 재사용합니다. 왼쪽은 영상 박스, 오른쪽은 `.script-box`; 상세 리포트에서는 영상 최소 높이를 200-220px로 낮춥니다.
- 지원자에게 공개되는 채용 결과 화면은 세부 점수 대신 상태 패널과 제한된 피드백 패널만 둡니다. 기업용 상세 리포트와 지원자용 결과 화면의 정보 밀도를 분리합니다.

### 면접 진행 화면
- 면접 진행 화면은 일반 GNB를 사용하지 않고 `.iv-top`을 사용합니다. 높이 `56px`, 좌측 로고, 중앙 화면명, 우측 타이머 pill 구조입니다.
- 본문 `.iv-body`는 `padding:26px 32px 32px`. 첫 요소는 항상 질문 카드 `.q-card`이며 `padding:24px 26px`, `margin-bottom:18px`.
- 질문 카드 안에는 보조 메타 `.qm`, 진행 번호 `.qn`, 실제 질문 `.qt`를 위에서 아래로 배치합니다. 질문 본문은 20px/700/1.5.
- 답변 영역은 `.iv-grid`=`1.6fr 1fr`, `gap:18px`, `margin-bottom:20px`. 왼쪽은 카메라/영상, 오른쪽은 답변 상태 패널입니다.
- 영상 박스는 어두운 배경 `#101019`, 라운딩 `16px`, 최소 높이 `300px`. 녹화 배지는 좌상단 14px 위치에 pill로 고정합니다.
- 하단 조작은 `.toolbar`로 배치하고, 기본 순서는 "질문 음성 다시 듣기" -> Primary "답변 완료" -> "다음 질문"입니다.

### 모달 / 오버레이
- 회사 상세처럼 문맥을 유지해야 하는 상세 정보는 페이지 이동보다 모달을 사용합니다.
- 모달 스크림은 최소 높이 `560px`, 중앙 정렬, 패딩 `34px`, 배경은 `rgba(16,15,31,.45)` 오버레이입니다.
- 모달 패널은 `max-width:520px`, 라운딩 `20px`, 그림자 `0 24px 64px rgba(40,30,90,.18)`, `overflow:hidden`.
- 모달 내부의 보조 패널은 바깥 모달 그림자와 충돌하지 않도록 `box-shadow:none`을 적용합니다.

### 시스템 처리 / 플로우 화면
- 직접 사용자 화면이 아닌 처리 흐름은 `.grid-2` 안에 `.flow-card`를 반복합니다.
- `.flow-card`는 패딩 `20px`, 라운딩 `16px`, 제목 행에 번호 칩 26px을 둡니다. 긴 설명은 13.5px/1.7로 낮춰 정보성 문서처럼 읽히게 합니다.

### 반응형 규칙
- 와이어프레임 실제 CSS의 모바일 전환점은 `max-width:760px`입니다. 이 폭 이하에서 `.grid-2`, `.grid-3`, `.cards-2`, `.kpi-row`, `.mini-grid`, `.feat-grid`, `.choice-grid`, `.iv-grid`는 모두 1열로 접힙니다.
- 모바일 `.page` 패딩은 `22px 18px 32px`로 줄입니다.
- 모바일 GNB는 좌우 패딩 `16px`, 로고-메뉴 간격 `18px`, 링크 패딩 `0 9px`, 폰트 13px로 축약합니다. 실제 제품에서는 이 규칙을 햄버거/아코디언 메뉴로 대체해도 됩니다.

---

## 11. 화면별 재사용 조합

| # | 화면 | 재사용 배치 조합 |
|---|---|---|
| 01 | 랜딩 | `lnav` -> 중앙 히어로 -> 미니 UI 2열 -> 기능 3열 -> 클로징 배너 |
| 02 | 로그인 | `auth-wrap` + 444px 폼 카드 + 세그먼트 + 입력/비밀번호 inline + 전체폭 CTA |
| 03 | 회원가입 유형 선택 | 540px 폼 카드 + 선택 카드 2열 + 전체폭 CTA |
| 04 | 지원자 회원가입 | 540px 폼 카드 + 이메일/코드 inline + 비밀번호 2열 + 약관 태그 |
| 05 | 기업 회원가입 | 540px 폼 카드 + 담당자/회사명 2열 + 이메일 인증 + 비밀번호 2열 |
| 06 | 비밀번호 재설정 | 444px 폼 카드 + 이메일/코드 inline + 비밀번호 필드 세로 배치 |
| 07 | 공고 목록 | GNB + page-head 액션 + 필터 toolbar + `posting` 리스트 |
| 08 | 공고관리탭 | page-head + KPI 3열 + 선별 테이블 + 페이지네이션 |
| 09 | 지원자 관리 | 등록 폼 패널 -> 초대 발송 패널 -> 진행 상태 테이블 |
| 10 | 서류 평가 상세 | 평가 근거 표 -> 리포트/비교 2열 -> 수동 평가 폼 |
| 11 | 채용 리포트 상세 | 점수 표 -> 근거 박스 -> 영상/스크립트 2열 -> 보조 지표 태그 |
| 12 | 채용 공고 관리 | 프로젝트 생성 폼 -> JD 등록/업로드 폼 |
| 13 | 면접 관리 | 역량 태그 toolbar -> 평가 기준 표 -> 질문 저장 폼 -> 시간 설정 toolbar |
| 14 | 기업 마이페이지 | 기업 프로필 폼 -> 알림 설정 toolbar |
| 15 | AI 모의면접 시작 | 설정 3열 -> 시작 CTA -> 연습 이력 테이블 |
| 16 | AI 모의면접 진행 | `iv-top` + 질문 카드 + 영상/상태 2열 + 답변 toolbar |
| 17 | 모의면접 리포트 | 종합 피드백 -> 점수 표 -> 영상/스크립트 2열 |
| 18 | 회사 리스트 | 필터 패널 -> 채용공고 카드 2열 |
| 19 | 회사 상세 | 모달 스크림 + 520px 모달 + 회사 정보/JD 보조 패널 |
| 19-1 | 지원서 제출 | 요약 3열 + steps -> 2열 입력/업로드 -> 2열 설명/상태 -> 동의 -> 하단 액션 |
| 20 | 지원현황 | 지원현황 테이블 -> 선택 지원 건 상세/동의/장치 점검 패널 |
| 21 | 채용 AI 면접 진행 | `iv-top` + 질문 카드 + 영상/상태 2열 + 답변 toolbar |
| 22 | 채용 AI 면접 결과 | 상태 패널 -> 제한된 피드백 패널 -> 돌아가기 CTA |
| 23 | 지원자 마이페이지 | 이력서/포트폴리오 2열 -> 응시 안내 알림 테이블 |
| SYS | 시스템 처리 흐름 | `grid-2` + `flow-card` 반복 |

> 화면 하단 중앙의 다크 pill 스위처는 **검토용 네비게이션**입니다. 실제 배포 시 제거하세요.

---

## 12. 구현 메모

- 와이어프레임 원본은 `/Users/ssoming/Downloads/init_wireframe.html` 단일 HTML입니다. 실제 구현에서는 검토용 노트북 프레임을 제외하고 `.app` 내부 구조만 옮깁니다.
- 강조색은 항상 그라데이션(`--brand-grad`) 우선, 단색이 필요하면 `#5B57F2`/`#6E5BF6`.
- 새 인증 화면 추가 시: `--bg-tint` 글로우 -> `auth-wrap` -> `form-card` -> 인풋/버튼 토큰 순서로 재사용.
- 새 내부 화면 추가 시: GNB -> `.page` -> `.page-head` -> `.panel` 반복 구조를 우선 적용합니다.
- **글로우 사용 범위:** 랜딩·인증 등 진입 화면 전용. 로그인 후 내부 페이지는 깨끗한 흰 배경 + 상단 GNB 레이아웃.
- **지양:** 다크 배경, 과한 그라데이션 남발, 둥근 모서리+좌측 보더 액센트 박스, 이모지(브랜드 요소 아님).

---

## 13. 적용 변형 — 기업 채용(B) 글라스 / 애플 톤

기업 채용(B) 화면에 실제 적용된 변형입니다. §1~§12의 와이어프레임 기준을 토대로, 기업 포털을 조용한 업무용 대시보드 + 가벼운 글라스 톤으로 다듬은 결과입니다. C/D/E 화면은 기존 기준(§1~§12)을 유지하고, 합의 시 본 변형으로 확장합니다.

### 적용 범위
- **전역(기업 레이아웃):** GNB 헤더 반투명 글라스, 내부 페이지 평면 배경.
- **`.glass-page` 한정(현재 공고 목록):** 패널/공고 카드 글라스, 애플 버튼, 입력 필드, 상태 칩. 다른 B 화면 확장 시 같은 토큰을 재사용합니다.

### 배경
- 내부 페이지 배경은 **평면 `--bg`**(`#F6F5FB`). 라디얼 글로우/그라데이션 배경은 사용하지 않습니다. (랜딩·인증의 글로우와 분리)

### GNB 헤더 (반투명 글라스, 플로팅 카드)
- 상단 sticky, `padding:14px 0 0`. 내부 바(`.gnb-inner`)는 폭 `min(1200px, 100%-32px)`, 높이 `62px`, 라운딩 `20px`의 플로팅 카드.
- 배경 `rgba(255,255,255,.58)` + `backdrop-filter: saturate(150%) blur(18px)`, 보더 `1px rgba(255,255,255,.6)`, 그림자 `0 16px 40px rgba(40,40,90,.12)`.
- 1차 메뉴: 활성/hover 시 텍스트 `--brand-strong` + 배경 칩 `--surface-soft`(라운딩 12px). 우측 아이콘 버튼은 반투명 `rgba(255,255,255,.6)` + blur.
- 메뉴는 `공고 목록` / `마이페이지`만 노출(기업 GNB 최소화).

### 카드 / 패널 (글라스)
- **위계:** 평면 배경 → **틴트 패널(컨테이너)** → **흰 카드(목록 행)**. 컨테이너에 옅은 색을 깔고 그 위에 흰 카드를 띄워 밋밋함(전부 흰색)을 피합니다.
- `.glass-page .panel`(컨테이너): 배경 **옅은 라벤더-블루 틴트** `linear-gradient(160deg, rgba(228,230,249,.72), rgba(238,242,250,.6))` + `saturate(160%) blur(20px)`, 보더 `1px rgba(255,255,255,.6)`, 라운딩 **22px**, 그림자 2겹 `0 1px 2px rgba(20,18,45,.04), 0 14px 36px rgba(40,40,90,.10)`.
- `.glass-page .posting`(목록 행 카드): 배경 **흰색** `rgba(255,255,255,.92)`, 보더 `1px rgba(255,255,255,.9)`, 라운딩 **18px**, 그림자 `0 1px 2px + 0 8px 22px`. hover 시 `#fff` + `translateY(-2px)` + 그림자 강화.
- 카드 안 카드 중첩 금지(기존 원칙 유지).

### 버튼 (애플 톤)
- 공통: 높이 **44px**, 패딩 `0 20px`, 라운딩 **16px**, 폰트 `14px/600`, `letter-spacing:-0.01em`. 전이 `cubic-bezier(.2,.8,.2,1)`, 누름 `:active { transform: scale(.97) }`.
- **Primary:** 그라데이션이 아니라 **단색** `--brand-strong`(`#5B57F2`) + 그림자 `0 6px 16px rgba(91,87,242,.32)`, hover `#6A64F4`.
- **Secondary:** 틴티드 그레이 `rgba(120,120,140,.14)`(보더 없음), 텍스트 `#1F1D2B`, hover `rgba(120,120,140,.20)`.
- 한 화면 안 액션 버튼은 동일 굴곡도/높이/글씨로 통일(Primary·Secondary는 색만 차이).
- 참고: 본 변형은 §4의 pill(999px) 대신 **16px**를 표준으로 씁니다(업무용 톤).

### 입력 필드
- 높이 **44px**, 라운딩 **16px**(버튼과 통일), 배경 `rgba(255,255,255,.85)`, 보더 `--line`(평면 배경에서도 보이도록 흰 반투명 보더 금지), `blur(6px)`. 포커스 링은 기존 `rgba(110,91,246,.14)` 유지.

### 상태 칩 (Badge)
- 프로스티드 칩: 배경 `rgba(255,255,255,.62)` + `blur(6px)`, 보더 `1px rgba(255,255,255,.7)`, 폰트 `600/-0.01em`, 좌측 **상태 점**(`::before`, 7px, `currentColor`).
- 색 의미는 §9 규칙 유지(success/warning/danger/neutral/info).
- 공고 상태 라벨(한글): `OPEN→모집중`, `DRAFT→작성중`, `CLOSING_SOON→마감임박`, `CLOSED→마감`, `ARCHIVED→보관`. 매핑 없는 상태는 원문 노출.

### 위치 인지
- 페이지 상단 긴 가이드 문구 대신 **breadcrumb + 제목**으로 현재 위치를 표시합니다. eyebrow(영문 라벨) 제거.

---

## 체크리스트 — 새 페이지 만들 때

- [ ] 검토용 셸(`device`, `chrome`, `review-nav`)이 아니라 `.app` 내부 배치만 구현했는가
- [ ] 내부 페이지면 상단 GNB(64px) + 본문(max 1200px) 골격을 따랐는가 (좌측 사이드바 금지)
- [ ] 하위 메뉴는 GNB 호버 드롭다운으로 노출했는가 (트리거+패널 한 래퍼로 묶어 끊김 방지)
- [ ] 페이지 헤더 뒤에 패널을 18px 리듬으로 쌓았는가
- [ ] 폼은 `.grid-2`/`.grid-3`, 목록은 `.posting`/`.jobcard`, 리포트는 점수 표 + 근거 + 영상/스크립트 패턴을 재사용했는가
- [ ] 면접 진행 화면은 GNB 대신 `iv-top` + `iv-body` 구조를 사용했는가
- [ ] 간격이 4px 스케일(8/12/16/24/32…) 안에 있는가
- [ ] 760px 이하에서 주요 그리드가 1열로 접히는가
- [ ] 상태 표현에 §9 Badge 색 규칙을 썼는가
- [ ] 인풋 포커스 링(`rgba(110,91,246,.14)`), 버튼 그림자 토큰을 재사용했는가
- [ ] 내부 페이지에 글로우를 넣지 않았는가
- [ ] 로고는 `assets/logo-init.png`를 비율 고정해 썼는가
