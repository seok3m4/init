{
  "name": "init-ai-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node ../api/node_modules/typescript/bin/tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/worker-runner.test.js"
  }
}
